# Claude Code Instructions for PolarBERT

## Environment Setup

Before running any Python scripts, activate the conda environment:

```bash
conda activate polarbert
```

The `polarbert` package is installed in the environment, so no PYTHONPATH manipulation is needed.
